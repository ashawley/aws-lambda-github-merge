name: npm

on:
  workflow_run:
    workflows: [sbt]
    types: [completed]

jobs:
  npm:

    runs-on: ubuntu-latest

    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
    - uses: actions/checkout@v3
    - uses: actions/github-script@v6
      with:
        script: |
          let allArtifacts = await github.rest.actions.listWorkflowRunArtifacts({
             owner: context.repo.owner,
             repo: context.repo.repo,
             run_id: context.payload.workflow_run.id,
          });
          let matchArtifact = allArtifacts.data.artifacts.filter((artifact) => {
            return artifact.name == "aws-gh-prs-assembly-0.5.2-SNAPSHOT"
          })[0];
          let download = await github.rest.actions.downloadArtifact({
             owner: context.repo.owner,
             repo: context.repo.repo,
             artifact_id: matchArtifact.id,
             archive_format: 'zip',
          });
          let fs = require('fs');
          fs.writeFileSync(`${process.env.GITHUB_WORKSPACE}/aws-gh-prs-assembly-0.5.2-SNAPSHOT.zip`, Buffer.from(download.data));

    - run: mkdir target/scala-2.12
    - run: 'unzip "aws-gh-prs-assembly-0.5.2-SNAPSHOT.zip" -d target/scala-2.12'
    - uses: actions/setup-node@v3
      with:
        node-version: 18
    - run: npm ci
    - run: npx sls package
